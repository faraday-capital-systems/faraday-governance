# Faraday Agent Standard Template

Agents must pin governance through `.faraday/governance.yml`, fail closed when
validation fails, avoid secrets and instance data in definition repos, and record
product-side evidence in the configured evidence path.
